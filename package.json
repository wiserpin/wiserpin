{
  "name": "wiserpin",
  "version": "0.1.0",
  "private": true,
  "description": "WiserPin - Pin smarter. Get wiser.",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "@vitest/coverage-v8": "^4.0.5",
    "@vitest/ui": "^4.0.5",
    "fake-indexeddb": "^6.2.4",
    "happy-dom": "^20.0.10",
    "turbo": "^2.2.3",
    "vitest": "^4.0.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
